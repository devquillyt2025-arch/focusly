.vercel
node_modules
dist
dist-ssr
*.local
.env*
.DS_Store
supabase/.temp
# Vite optimizer cache stamps (auto-generated, should never be committed)
vite.config.js.timestamp-*.mjs
# Dev scratch files committed accidentally
temp*.css
temp*.js
temp_script.cjs
scratch-*.css
scratch-*.js
# Next.js landing build output
landing/.next
landing/node_modules
# MCP server config — holds a Supabase personal access token in plaintext.
# Project-scoped and machine-local; never commit it.
.mcp.json
